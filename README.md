A library to compress torch tensors using Google's snappy compression library.

First you need snappy:
On ubuntu: `apt-get install libsnappy-dev`
On mac: `brew install snappy`

Then `luarocks install torch-snappy`
